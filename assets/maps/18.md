# Promyvion-Dem (zone 18)

## Status and sources

This is a routing-first **partial** map. It publishes native Detour topology
for the exact Spire of Dem threshold, the scripted default floor-one arrival,
Satiator's two exact initial-spawn possibilities, and every additional
component proved public by a nonzero ordinary-mob SQL record. It authors no
Memory Stream or Memory Receptacle relocation, portal-state edge, return warp,
drop, transition, blocked link, mask, elevation band, or page rule.

The stock catalog contains only `018_00`, page `0`; use
`stock_calibration = true`. Sources are xiNavmeshes commit
`d5de48de84868bde744e4864768a611e5aad82b0`, CatsEyeXI commit
`314deaf03465f2b24b6a1e4e73a016ca036f1084`, and
`assets/world/connections.lua`. `Promyvion-Dem.nav` SHA-256 is
`1CB007BF7A2B71BF1BE9300C486F2F1510DEE4301B5EA7A2623D1951BAC1B5AF`.
CatsEye SQL/script `(x, vertical, z)` becomes live `(x, y=z, z=vertical)`.

## Exact supported evidence

The two Dem-side Spire threshold samples are retained in connection-catalog
source order:

```text
(20.000,59.998,-2.174) outbound to zone 19
(20.000,63.031,-2.100) arrival from zone 19
```

The zone script's zero-coordinate fallback places the player at the exact
floor-one arrival `(185.891,-52.331,0.000)`. There is no active Home Point,
Survival Guide, fixed Treasure Chest, or fixed Treasure Coffer record.

Satiator is the only supported NM. It is level 38, uses a randomized
70-to-75-minute timed respawn, and preserves its script array in source order:

```text
(-44.317,-242.987,0.000)
(-205.880,268.850,-0.500)
```

The selector then adds the first SQL source-order ordinary-mob point for the
only otherwise-unselected public component:

```text
Weeper (-233.240,-40.463,-0.500)
```

Memory Receptacles, their Strays, eleven state-selected Memory Streams,
return warps, Memory Fluxes, mission-only custom mobs, and TODO Apex rows are
excluded as encounter or transport mechanisms rather than ordinary walkable
connectors. The map-QM script documents only two known positions and
explicitly says its moving-position catalog is incomplete, so it is not
published as a static reference or selector. Zero-coordinate records are
excluded.

## Component and topology audit

The unseeded native graph has 5,646 nodes, 5,719 connections, and 711
components. It includes 461 isolated nodes, five native edges longer than 24
yalms, no edge steeper than four yalms, twenty projected separated-floor
pairs, and no substantial nearby-component advisory within eight yalms.

Production retains all five large source-backed networks: 4,648 nodes, 5,427
bidirectional connections, and five components with sizes:

```text
1682,1215,926,467,358
```

The 706 excluded components have no exact supported destination or nonzero
ordinary-mob proof point; none is larger than four nodes. Production has no
isolated node, retains the five source-native long edges, and has no steep
edge, projected separated-floor overlap, or substantial nearby-component
advisory within eight yalms. Long edges remain attended review items; no
connection is inferred from proximity.

## Exact generator job

Add in numeric zone order:

```python
GraphJob(
    "Promyvion-Dem.nav",
    "18.lua",
    (
        "--zone-id", "18",
        "--seed=20.000,59.998,-2.174",
        "--seed=20.000,63.031,-2.100",
        "--seed=185.891,-52.331,0.000",
        "--seed=-44.317,-242.987,0.000",
        "--seed=-205.880,268.850,-0.500",
        "--seed=-233.240,-40.463,-0.500",
    ),
),
```

Register in numeric order:

```lua
[18] = 'assets/paths/18.lua',
```

## Exact catalog entry

Add in numeric zone order:

```lua
[18] = {
    name = 'Promyvion-Dem',
    stock_calibration = true,
    travel_references = travel_reference_set({}),
    treasure_spawns = {},
    nm_spawn_ranges = {
        { name = 'Satiator', level = '38', z = 0.000, floor = 'UNASSIGNED', spawn_type = 'Timed', placeholder_count = 0, points = {
            { x = -44.317, y = -242.987 }, { x = -205.880, y = 268.850 },
        } },
    },
},
```

README rows, in numeric order:

```text
| Promyvion-Dem | `1CB007BF7A2B71BF1BE9300C486F2F1510DEE4301B5EA7A2623D1951BAC1B5AF` |
| `18.lua` | 4,648 | 5,427 | `DEE9DC31F1797B33B8A8D7F058C9CF1B075F5028E6FFEFE0310610A2507D04D7` |
```

## Validation and attended blockers

Two native generations and the inferred-adjacency generation are
byte-identical; graph SHA-256 is
`DEE9DC31F1797B33B8A8D7F058C9CF1B075F5028E6FFEFE0310610A2507D04D7`.
The validator passes with zero one-way connections. All six exact selectors
snap within 2.551 yalms, and the Spire threshold pair routes 4.400 yalms.

The default arrival to a floor-one Wanderer routes 105.683 yalms; Satiator's
south possibility to a south-network Weeper routes 272.856; its north
possibility to a north-network Weeper routes 212.374; the Spire threshold to
a fourth-network Seether routes 557.524; and the retained ordinary proof
Weeper to a second exact ordinary point routes 20.543.

Attended completion requires live stock calibration; exact page/floor
behavior; all eleven Memory Streams and their active/inactive portal states;
the five return/exit trigger areas; Memory Receptacle combat and relocation;
Memory Flux behavior; all drops and one-way behavior; the incomplete moving
map-QM possibilities; the five long native edges; classification of the 706
excluded fragments if one affects a desired route; Satiator marker-floor
behavior; endpoint and off-route recovery; and in-game reload. No shared
integration file is edited by this pass.
