# Buburimu Peninsula (zone 118)

## Routing-first completion

The exact runtime stock calibration for page `0` remains authoritative. The
graph joins the public terrain to the valid Bibiki threshold plateau through
one direct native-neighbor seam; no structure image is required.

- Graph: 10,129 nodes, 11,664 bidirectional edges, 1 component.
- SHA-256: `EF5E8D56AECD85593F24C8A8CADE09D033E32BAD489E5B6282222AF6768D3E73`.
- Seed: Survival Guide `(-485.7, 46, -32)`.
- Nav source: xiNavmeshes `d5de48de84868bde744e4864768a611e5aad82b0`;
  SHA-256 `39FBE1BA24BE53743AC2DB534B7F241F6567DCDB9E9CB13601D2CD14DFDCD915`.

Tahrongi, Mhaura, Shakhrami, and Bibiki routes resolve. The raw Dynamis
reverse-zoneline endpoint belongs to unrelated elevated collision and is not
seeded; the attended Hieroglyphics NPC is the truthful interaction landmark.
Two generations and native/inferred adjacency are byte-identical.

## Deterministic CatsEyeXI source

CatsEyeXI commit `314deaf03465f2b24b6a1e4e73a016ca036f1084` is the pinned
authority. Zone `118` was audited exhaustively in `sql/mob_groups.sql` and
`sql/mob_spawn_points.sql`, then against `IDs.lua`, `Zone.lua`, and every zone
mob script. Source tuples are `(x, vertical, z)`; catalog points use map
`(x, y=z)` and retain a representative vertical value in each range's `z`.

The same source supplies Survival Guide unlock bit `33`; there is no fixed
standard treasure set.

## Notorious-monster catalog

The complete active, supportable static catalog is:

| Name | Level | Type | Effective starts | Placeholder count |
|---|---:|---|---:|---:|
| Wake Warder Wanda | 22-23 | Timed/random start (60-70 minutes) | 8 | 0 |
| Helldiver | 29-30 | Lottery/random start | 50 | 1 Zu |
| Buburimboo | 30-31 | Lottery/random start | 50 | 1 Shoal Pugil |
| Backoo | 37 | Timed/daytime | 1 | 0 |

The scripted arrays are copied in source order. Wake Warder Wanda's script
assigns the same eight-point `spawnPoints` table twice; the second assignment
replaces the first, so the effective catalog contains eight possibilities,
not sixteen. Its script randomizes both position and a 60-70-minute respawn.
Helldiver and Buburimboo each provide 50 possibilities; their `phList` tables
identify one immediately preceding placeholder apiece. Backoo uses the fixed
SQL position `(-409.210,-219.861,-7.740)` and `Zone.lua` restricts it to
Vana'diel hours 06:00-16:00; its mob script applies a 60-90-minute respawn.

The following are deliberately excluded:

- Botulus Rex's three nonzero records are Unity Wanted/event actors, not
  ordinary static initial spawns.
- Abyssdiver's three zero-level nonzero records are event-controlled actors
  without active ordinary-spawn behavior in this zone.
- Ketos is an ordinary Fields of Valor target despite its special group row.
- Hobgoblins, Theoyagudo, pets, garrison groups, fished groups, Pixie, and
  other ordinary mobs are not notorious-monster destinations.

## Routing and validation

Helldiver's 50 points snap from `0.166` to `11.890` yalms; Buburimboo's 50
points snap from `0.354` to `9.329` yalms; Backoo snaps at `5.492` yalms. All
101 therefore route on the existing single connected component.

Wake Warder Wanda's eight points are offshore at live vertical `16-20`. They
do not represent player-walkable terrain: their nearest retained land nodes
are `22.668-96.886` planar yalms away at live vertical near `-0.3..0.9`, while
the graph nodes within four vertical yalms are over 476 yalms away. The eight
truthful spawn markers therefore remain marker-only. Seeding water or joining
it to shore would invent player traversal, so no `GraphJob`, path graph, or
transition change is made.

The focused path validator and zone-118 catalog audit pass. Production and
native topology match at 10,129 nodes, 11,664 bidirectional edges, and one
component; inferred topology has no delta or candidate component pair. An
exact scripted-array/catalog comparison and `git diff --check` also pass.
