# Dragon's Aery (zone 154)

## Status

This is a routing-first **partial** map. It publishes deterministic native
Detour topology for both source-backed zone handoffs, the Survival Guide, the
ordinary public mob surfaces, and the Fafnir/Nidhogg basin without requiring
or generating a structure image. No transition or blocked-link exception is
authored.

The stock catalog contains one page, page `0`. Runtime stock calibration is
authoritative through `stock_calibration = true`; the exact numeric map-table
record was not captured during this unattended pass.

## Deterministic sources

- xiNavmeshes checkout:
  `d5de48de84868bde744e4864768a611e5aad82b0`
- `Dragons_Aery.nav` SHA-256:
  `A54BE0DAD0589CC7AE785F35A360605A683763EBFB035970378122671AD9D7F1`
- CatsEyeXI checkout:
  `314deaf03465f2b24b6a1e4e73a016ca036f1084`
- Exact physical handoffs: `assets/world/connections.lua`
- Travel, treasure, normal-mob, and HNM evidence: CatsEyeXI
  `sql/npc_list.sql`, `sql/mob_groups.sql`, `sql/mob_spawn_points.sql`,
  `scripts/globals/teleports/survival_guide_map.lua`, the active treasure
  table, and `scripts/zones/Dragons_Aery/`

CatsEyeXI SQL and spawn-script tuples are `(x, vertical, z)`. Graph and map
records use live `(x, y, z)`, so source `z` becomes world `y`.

## Stock page, travel, and treasure

The imported stock catalog identifies only `m_154_00`.

- Survival Guide 49: `(-60.330,-33.690,-1.170)`, unlock bit `29`
- Home Points: verified empty
- Treasure Chests and Coffers: verified empty in CatsEyeXI's active fixed
  treasure table

There are no fixed casket/chest/coffer destinations. Event and zero-position
actors are not promoted.

## Exact physical handoffs

Both Dragon's Aery-side points of both pinned bidirectional handoffs are
retained:

```text
The Boyahda Tree:         (-60.006,-42.501,-4.748) (-60.006,-39.501,-2.915)
The Sanctuary of Zi'Tah: (121.480,139.987,-13.450) (118.378,140.068,-11.935)
```

## Supported HNMs

Fafnir and Nidhogg are forced from the active `qm0` record and each has an
exact 50-point era-HNM spawn array in its pinned zone script. The catalog
should preserve every X/Y point in source order with `spawn_type = 'Fixed'`,
`placeholder_count = 0`, `floor = 'SURFACE'`, and representative platform
`z = 6.800`. The source vertical values range narrowly around `6.0..7.4`.

All 100 source positions snap to the retained HNM component within the
24-yalm runtime radius; the largest reviewed three-dimensional miss is 9.967
yalms. Fished records and ordinary mobs are not published as NM references.

## Native-component review

The unseeded native graph has 239 nodes, 227 connections, and 16 components.
Native and inferred unseeded outputs are byte-identical. Seeding every normal
non-fished mob point selects the same graph as the minimal production selector:
207 nodes, 208 connections, and three components.

| Nodes | Evidence and disposition |
|---:|---|
| 108 | Boyahda threshold, Survival Guide, and ordinary western/central spawn surfaces |
| 37 | Fafnir/Nidhogg basin and its ordinary Darter spawns |
| 62 | Zi'Tah threshold and northern ordinary spawn surfaces |

The thirteen excluded components contain no exact handoff, travel reference,
HNM point, or normal non-fished spawn. The largest has eleven nodes and lies
near the central surface; it is not promoted from proximity. The rest have at
most three nodes each. Their collision roles remain unverified.

The selected graph has no isolated nodes, no stacked-floor overlaps, no edge
longer than 24 yalms, and no edge steeper than four yalms. The auditor reports
one substantial-component advisory:

```text
north 62 <-> HNM 37:
(96.500,59.000,-7.788) <-> (98.167,50.000,-5.655)
planar 9.153, elevation 2.133
```

A direct closest-pair review gives the more planar-nearest north/HNM lead at
`(96.500,59.000,-7.788) <-> (95.300,54.200,-6.348)`, planar `4.948`,
elevation `1.440`. The main/HNM lead is
`(63.000,25.500,-5.688) <-> (68.333,24.167,-6.922)`, planar `5.497`,
elevation `1.234`. Neither gap proves a passable ramp or threshold. Both stay
disconnected until attended entrance/midpoint/exit evidence supports a narrow
exception.

## Deterministic generation

```text
python tools/generate_path_graph.py <xiNavmeshes>/Dragons_Aery.nav \
  assets/paths/154.lua --zone-id 154 \
  --seed=-60.006,-42.501,-4.748 --seed=-60.006,-39.501,-2.915 \
  --seed=121.480,139.987,-13.450 --seed=118.378,140.068,-11.935 \
  --seed=-60.33,-33.69,-1.17 --seed=78,39,6 --seed=78,34,6
```

No mask, elevation bound, transition, one-way transition, or blocked link is
used.

## Validation

- Two native generations are byte-identical.
- Production graph SHA-256:
  `7D38EFF938BDC4E0CD37901C1C866FF81E4ECE6E7A17E238B8247E3B281FFF1A`
- Native and inferred selected outputs are byte-identical: 207 nodes and 208
  bidirectional connections.
- `tools/validate_path_graphs.py assets/paths/154.lua` passes.
- All handoff and travel seeds snap within 3.410 yalms. The two initial HNM
  seeds snap within 8.327 yalms.
- Boyahda departure to Survival Guide has a 9.466-yalm native route.
- Fafnir initial position to Nidhogg initial position has a 27.136-yalm native
  route.
- Survival Guide to HNM basin and HNM basin to Zi'Tah correctly have no route
  across the two unresolved native gaps.

The official focused catalog auditor must be rerun after the shared GraphJob
is registered. Numeric stock calibration, both route seams, excluded-component
review, endpoint behavior, off-route recovery, and in-game reload remain
attended-only gates.

## Required shared integration

Add this path registration:

```lua
[154] = 'assets/paths/154.lua',
```

Add a `GraphJob` for `Dragons_Aery.nav`, output `154.lua`, with the exact seven
generation arguments above. Add this catalog record:

```lua
[154] = {
    name = "Dragon's Aery",
    stock_calibration = true,
    travel_references = travel_reference_set({
        { kind = 'survival_guide', name = 'Survival Guide', unlock_bit = 29,
          x = -60.330, y = -33.690, z = -1.170 },
    }),
    treasure_spawns = {},
    nm_spawn_ranges = {
        -- Fafnir: Fixed, placeholder_count 0, z 6.800, SURFACE, exact
        -- 50-point scripts/zones/Dragons_Aery/mobs/Fafnir.lua X/Y array.
        -- Nidhogg: Fixed, placeholder_count 0, z 6.800, SURFACE, exact
        -- 50-point scripts/zones/Dragons_Aery/mobs/Nidhogg.lua X/Y array.
    },
},
```

Copy both HNM arrays exactly in source order, converting each script point
from `{ x, y = vertical, z = horizontal }` to catalog `{ x, y = horizontal }`.
No shared file is edited by this zone-specific pass.
