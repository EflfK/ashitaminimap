# Hall of the Gods (zone 251)

## Status

This is a routing-first **partial** map. It publishes native Detour topology
for the two exact physical zone handoffs, the Cermet Grate, and both
Shimmering Circle endpoints without requiring or generating a structure
image. No transition or blocked-link exception is authored.

The stock catalog contains one page, page `0`. Runtime stock calibration is
authoritative through `stock_calibration = true`; the exact numeric map-table
record was not captured during this unattended pass.

## Deterministic sources

- xiNavmeshes checkout:
  `d5de48de84868bde744e4864768a611e5aad82b0`
- `Hall_of_the_Gods.nav` SHA-256:
  `8717929CA7EFF7296FEA4C577C320FE285A7D0959CC6CB2678DE7C1C8443EB24`
- CatsEyeXI checkout:
  `314deaf03465f2b24b6a1e4e73a016ca036f1084`
- Exact physical handoffs: `assets/world/connections.lua`
- Exact mechanism and NPC records: CatsEyeXI `sql/npc_list.sql` and
  `scripts/zones/Hall_of_the_Gods/`
- Travel, treasure, and mob categories: CatsEyeXI Home Point, Survival Guide,
  active treasure, mob-group, and mob-spawn sources

CatsEyeXI SQL tuples are `(x, vertical, z)`. Graph and map records use live
`(x, y, z)`, so source `z` becomes world `y`.

## Stock page and destinations

The imported stock catalog identifies only `m_251_00`. There are no active
Home Points, Survival Guides, fixed Treasure Chests, fixed Treasure Coffers,
or mob groups/NM spawn records for this zone. Dynamic, event-only, and
zero-position cutscene actors are not map destinations.

Exact active mechanism records are:

```text
Cermet Grate:             (0.000,50.201,-14.302)
Shimmering Circle, lower: (0.066,149.316,-20.693)
Shimmering Circle, upper: (0.026,250.731,0.109)
```

The lower circle's pinned script lifts qualified players to the upper area;
the upper circle triggers the return event. This proves a scripted mechanism,
not a normal walkable Detour edge. AshitaMiniMap's current map-owned travel
schema has no Cermet-Grate or Shimmering-Circle marker kind, so these points
are routing/component evidence rather than published Home Point/Guide records.

## Exact physical handoffs

Both Hall-side points of both pinned bidirectional handoffs are retained:

```text
Ro'Maeve:       (0.004,-180.057,-3.526) (-0.011,-176.133,-1.848)
Ru'Aun Gardens: (0.000,365.058,-3.316)   (0.005,362.018,-1.640)
```

## Native-component review

The unseeded native graph has 512 nodes, 546 connections, and 27 components.
Native and inferred unseeded outputs are byte-identical. The production
selector retains 289 nodes, 322 connections, and four components:

| Nodes | Evidence and disposition |
|---:|---|
| 181 | Ro'Maeve threshold and southern approach, world Y `-219.5..-13.7` |
| 29 | Cermet Grate and central/northern corridor, world Y `10.5..139.6` |
| 5 | Lower Shimmering Circle landing, world Y `142.9..153.8` |
| 74 | Upper Shimmering Circle through Ru'Aun threshold, world Y `242.5..389.4` |

The 23 excluded components contain no exact handoff or active mechanism
point. Several lie close to the central corridor at different elevations and
may be decorative under-floor collision, side ledges, grate pieces, or route
fragments. They are not selected from proximity alone. In particular, the
50-node component at Detour elevation `-7.6`, the paired 34/26-node strips at
elevation `10`, and the 39/21-node platforms near the lower circle require
live/full-graph classification before they can be described as player-route
surfaces.

The selected graph has no isolated nodes, no stacked-floor overlaps, and no
nearby-component advisory under the auditor's substantial-component rule. It
has one 28-yalm native edge from `(-0.000,87.421,16.600)` to
`(-0.000,59.421,12.600)` whose computed elevation delta is exactly four yalms
(floating-point comparison reports just above four). It is retained native
source topology, not an authored transition.

## Unresolved mechanisms

- The Ro'Maeve approach and Cermet corridor are separated around the Cermet
  Grate. The source confirms the gated interaction but does not prove which
  navmesh fragments form the passable route after qualification.
- The 29-node corridor ends about three yalms before the five-node lower-circle
  landing. No link is inferred.
- The lower and upper Shimmering Circles are a scripted lift/return pair. A
  display route across that mechanism requires attended endpoint behavior and
  page/route semantics; it is not represented as a walkable edge.

## Deterministic generation

```text
python tools/generate_path_graph.py <xiNavmeshes>/Hall_of_the_Gods.nav \
  assets/paths/251.lua --zone-id 251 \
  --seed=0.004,-180.057,-3.526 --seed=-0.011,-176.133,-1.848 \
  --seed=0,365.058,-3.316 --seed=0.005,362.018,-1.640 \
  --seed=0,50.201,-14.302 --seed=0.066,149.316,-20.693 \
  --seed=0.026,250.731,0.109
```

No mask, elevation bound, transition, one-way transition, or blocked link is
used.

## Validation

- Two native generations are byte-identical.
- Production graph SHA-256:
  `770300794C8655A48D884A400EBB87D8F847E9AC4F8A106F5463EFA2E15169D3`
- Native and inferred selected outputs are byte-identical: 289 nodes and 322
  bidirectional connections.
- `tools/validate_path_graphs.py assets/paths/251.lua` passes.
- All seven exact seed coordinates snap within 5.145 yalms; both circles snap
  within 0.430 yalms.
- The Ro'Maeve handoff pair has a 4.117-yalm native route.
- Upper circle to Ru'Aun departure has a 125.605-yalm native route.
- Ro'Maeve to Cermet Grate, Cermet Grate to lower circle, and lower to upper
  circle correctly have no route across unresolved mechanisms.

The official focused catalog auditor must be rerun after the shared GraphJob
is registered. Numeric stock calibration, the Cermet route, lower-circle
landing, scripted lift behavior, excluded central fragments, off-route
recovery, and in-game reload remain attended-only gates.

## Required shared integration

Add this path registration:

```lua
[251] = 'assets/paths/251.lua',
```

Add a `GraphJob` for `Hall_of_the_Gods.nav`, output `251.lua`, with the exact
seven generation arguments above. Add this catalog record:

```lua
[251] = {
    name = 'Hall of the Gods',
    stock_calibration = true,
    travel_references = travel_reference_set({}),
    treasure_spawns = {},
    nm_spawn_ranges = {},
},
```

No shared file is edited by this zone-specific pass.
