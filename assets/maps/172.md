# Zeruhn Mines (zone 172)

## Status

This is a routing-first **partial** map. It publishes native Detour topology
for the main mine network and the exact Korroloka zoning threshold. It
requires no structure image and authors no transition or blocked-link
exception.

Two source questions block a completion claim:

- the 74-node Korroloka threshold does not join the 837-node main network; and
- an unselected 173-node interior strip lies close to the main network but has
  no exact handoff or supported destination proving player accessibility.

Neither component is joined from proximity. The interior strip remains
unresolved until collision inspection or focused live traversal proves
whether it is a public walkway, mine rail surface, or inaccessible geometry.

## Page and floor scope

The imported stock catalog identifies one page, `m_172_00`. No alternate stock
page is present. The selected graph preserves Detour elevation in 3D, has no
stacked-floor overlaps, and does not flatten the deeper Korroloka threshold
into the main mine network.

Runtime stock calibration should remain authoritative through
`stock_calibration = true`. The installed `FFXiMain.dll` did not expose the
expected static table signature and AshitaMCP was unavailable during this
audit, so the exact page-0 scale byte and signed offsets remain unresolved.
This also prevented live close/overview calibration and off-route recovery
tests.

## Deterministic sources

- xiNavmeshes checkout:
  `d5de48de84868bde744e4864768a611e5aad82b0`
- `Zeruhn_Mines.nav` SHA-256:
  `AC94203C5DC8AE287741F001D84CFC77A589F1EF4612CA4C0FC0B1FCD77B0D32`
- CatsEyeXI checkout:
  `314deaf03465f2b24b6a1e4e73a016ca036f1084`
- World handoffs: `assets/world/connections.lua`
- Travel and destination records: CatsEyeXI
  `scripts/globals/teleports/survival_guide_map.lua`,
  `scripts/zones/Zeruhn_Mines/IDs.lua`, `sql/npc_list.sql`,
  and `sql/mob_groups.sql`

## Native-component review

The unseeded native graph has 1,390 nodes, 1,375 edges, and 106 components.
The production selector retains 911 nodes, 997 edges, and two components:

| Nodes | World bounds | Detour elevation | Evidence and disposition |
|---:|---|---|---|
| 837 | `x=-161.3..145.9`, `y=-345.5..74.5` | `-11.1..1.0` | Main mine network; Survival Guide and Bastok Mines handoff |
| 74 | `x=-306.1..-168.2`, `y=-22.5..21.5` | `-15.7..-1.6` | Korroloka threshold; retained separately pending live seam evidence |

The 104 excluded components contain no exact handoff or supported fixed
destination. Most are small collision fragments. One substantial component
requires explicit review:

| Nodes | World bounds | Detour elevation | Disposition |
|---:|---|---|---|
| 173 | `x=-113.6..-86.8`, `y=-156.2..236.3` | `-5.3..-3.5` | Unresolved long narrow interior strip; not seeded without accessibility evidence |

The selected graph reports one nearby-component lead:

```text
main network:       (-161.316,19.875,0.221)
Korroloka threshold:(-168.941,19.750,-2.079)
planar gap:         7.626 yalms
elevation gap:      2.300 yalms
```

The full-source audit also reports the unresolved 173-node strip near the main
network:

```text
interior strip: (-106.649,57.000,-5.079)
main network:   (-105.566,59.250,-1.479)
planar gap:     2.497 yalms
elevation gap:  3.600 yalms
```

The elevation values are Detour elevation, not live player Z. Neither lead is
proof of a staircase, ramp, or walkable seam.

## Exact handoff seeds

The selector retains both Zeruhn sides of each paired handoff:

```text
Bastok Mines:     ( 23.039,  3.851,-2.622) ( 20.510,  4.019,-1.583)
Korroloka Tunnel: (-270.707,-20.268,14.159) (-273.927,-20.256,12.872)
```

## Supported destinations

- Survival Guide: `(-10.050,5.810,0.000)`, unlock bit/guide ID `21`
- Home Points: verified empty in the active CatsEyeXI NPC records
- Fixed Treasure Chests: verified empty
- Fixed Treasure Coffers: verified empty
- Relevant fixed NMs: verified empty in the pinned zone IDs and active fixed
  spawn records

The sixteen `Treasure_Casket` rows use dynamic zero coordinates and are not
static treasure destinations.

## Deterministic generation

The shared generator job is:

```python
GraphJob(
    "Zeruhn_Mines.nav",
    "172.lua",
    (
        "--zone-id", "172",
        "--seed=23.039,3.851,-2.622",
        "--seed=20.510,4.019,-1.583",
        "--seed=-270.707,-20.268,14.159",
        "--seed=-273.927,-20.256,12.872",
        "--seed=-10.05,5.81,0",
    ),
),
```

No mask, elevation band, transition, or blocked-link argument is used.

## Validation

- Two native generations are byte-identical.
- Production graph SHA-256:
  `4D316BB643E1A1394E783C079343D5E3B00409203F0744455F754AAB81F97CFC`
- Native and inferred generation are byte-identical: 911 nodes and 997
  bidirectional edges.
- `tools/validate_path_graphs.py assets/paths/172.lua` passes.
- Survival Guide to the Bastok Mines threshold: 28.882 yalms.
- The two Korroloka threshold records route to each other over 5.762 yalms.
- Survival Guide to Korroloka correctly has no route while the source seam
  remains unverified.
- Every supported endpoint snaps within 4.231 yalms.

`tools/audit_path_graphs.py` cannot audit this graph until the shared
`GraphJob` is integrated. The equivalent local native/inferred comparison and
component metrics above pass. Exact stock calibration, the Korroloka seam,
the 173-node interior strip, and live recovery remain blockers.

## Shared-catalog integration

Register:

```lua
[172] = 'assets/paths/172.lua',
```

Add:

```lua
[172] = {
    name = 'Zeruhn Mines',
    stock_calibration = true,
    travel_references = travel_reference_set({
        { kind = 'survival_guide', name = 'Survival Guide', unlock_bit = 21, x = -10.050, y = 5.810, z = 0.000 },
    }),
    treasure_spawns = {},
    nm_spawn_ranges = {},
},
```
