# Ranguemont Pass (zone 166)

## Status

This is a routing-first **partial** map. It publishes native Detour topology
for every exact physical handoff, the Survival Guide, and the three supported
NM references below. It creates no structure asset and authors no transition,
one-way transition, blocked link, page rule, or guessed connection.

The imported stock catalog has only page `0`, also its default page. Shared
integration should use `stock_calibration = true`; live stock metadata remains
authoritative for its exact origin and scale.

## Deterministic sources

- xiNavmeshes commit `d5de48de84868bde744e4864768a611e5aad82b0`
- `Ranguemont_Pass.nav` SHA-256
  `67FB01DA11383533F90DF800E2DAB3FCCC3D7F1A25BF1D1A610DB4315AECA302`
- CatsEyeXI commit `314deaf03465f2b24b6a1e4e73a016ca036f1084`
- Exact handoffs: `assets/world/connections.lua`
- Travel and NM evidence: pinned CatsEyeXI SQL,
  `scripts/globals/teleports/survival_guide_map.lua`, and Ranguemont zone
  scripts

CatsEye tuples `(x, vertical, z)` convert to live `(x, y=z, z=vertical)`.

## Components and focused audit

The complete native source has 2,317 nodes, 2,383 bidirectional edges, and 104
components. Exact destination selection retains 869 nodes, 935 bidirectional
edges, and five components:

```text
567, 221, 74, 5, 2
```

Every component contains exact supported evidence: the East Ronfaure side and
Survival Guide, Gloom Eye, the Beaucedine Glacier threshold, Tros, or
Taisaijin. Production has no isolated node, edge longer than 24 yalms, edge
steeper than four yalms, projected separated-elevation overlap, or substantial
nearby-component lead. Native topology still does not prove that a closed
door, drop, or narrow passage is player-traversable.

The unseeded graph's 104 components, 29 isolated nodes, and 26 substantial
nearby-component leads show why unrelated source fragments are excluded.

## Handoffs and travel

All four Ranguemont-side handoff endpoints are retained in source order:

```text
arrival from Beaucedine Glacier: (302.778,257.759,-68.131)
departure to East Ronfaure:      (-140.044,-324.570,0.000)
departure to Beaucedine Glacier: (305.885,258.022,-70.470)
arrival from East Ronfaure:      (-139.986,-321.508,2.162)
```

- Home Points: verified empty
- Survival Guide: `(-145.24,-298.79,4.37)`, menu index `41`, unlock bit `7`
- Fixed Treasure Chests: verified empty
- Fixed Treasure Coffers: verified empty

The zone has dynamic Treasure Caskets, but Ranguemont Pass has no entry in the
pinned active fixed-treasure table, so caskets are not promoted to fixed map
references.

Representative endpoint snaps pass: Beaucedine arrival `1.566`, Beaucedine
departure `1.896`, East Ronfaure arrival `1.259`, East Ronfaure departure
`0.734`, and Survival Guide `0.881` yalms. East arrival to East departure
routes for `2.634` yalms, and East arrival to the Survival Guide routes for
`26.815` yalms. The Beaucedine sides resolve to the same threshold node.

## Supported NMs

Use this exact catalog order:

```text
Gloom Eye; Taisaijin; Tros
```

- Gloom Eye, level `73-74`, lottery, placeholder count `1`: copy its complete
  one-point `entity.spawnPoints` array, `(-152.136,-89.814,-5.216)`.
- Taisaijin, level `60`, conditional timed conversion, placeholder count `3`:
  use its nonzero SQL point `(-15,-131,7)`. The three Taisai placeholder
  positions are mechanic evidence, not additional static Taisaijin starts.
- Tros, level `44`, quest-triggered: use its nonzero SQL point
  `(-289,212,-45)`.

Exclude ordinary mobs, pets, zero-position Metallic Slime, and Hyakume. The
pinned Hyakume group has zero levels and is tied to a Unity objective; it is
not promoted into this normal static-NM catalog.

All eight exact selector points snap. Gloom Eye snaps at `2.196`, Taisaijin at
`2.489`, and Tros at `1.195` yalms. Their components remain disconnected from
other supported components rather than receiving guessed links.

## Determinism and validation

- Native generation twice and inferred generation are byte-identical.
- Graph: 869 nodes, 935 bidirectional edges, five components.
- SHA-256:
  `5BAE03C7507D13674BF7CCE68C7F132B8A5AB9F85C05DB037381E79DDF008212`
- `tools/validate_path_graphs.py assets/paths/166.lua` passes.
- No transition, one-way transition, blocked link, mask, elevation bound, or
  page ID is authored.

## Exact shared integration

Register:

```lua
[166] = 'assets/paths/166.lua',
```

Add this exact generator job:

```python
GraphJob(
    "Ranguemont_Pass.nav",
    "166.lua",
    (
        "--zone-id", "166",
        "--seed=302.778,257.759,-68.131",
        "--seed=-140.044,-324.570,0.000",
        "--seed=305.885,258.022,-70.470",
        "--seed=-139.986,-321.508,2.162",
        "--seed=-145.24,-298.79,4.37",
        "--seed=-152.136,-89.814,-5.216",
        "--seed=-15.000,-131.000,7.000",
        "--seed=-289.000,212.000,-45.000",
    ),
),
```

Add the catalog entry after zone `162` and before zone `167`, with
`name = 'Ranguemont Pass'`, `stock_calibration = true`, the Survival Guide,
`treasure_spawns = {}`, and the three NM records in the exact order above. Do
not add page rules, door links, dynamic caskets, or destination page IDs.

Add this generated-artifact row after `162.lua` and before `167.lua`:

```text
| `166.lua` | 869 | 935 | `5BAE03C7507D13674BF7CCE68C7F132B8A5AB9F85C05DB037381E79DDF008212` |
```

## Attended blockers

- Confirm stock page `0`, calibration, and all marker alignment live.
- Traverse the NPC-operated door by Myffore near
  `(-179.951,-172.234,4)` and verify whether it affects any retained route.
- Inspect all five destination-backed components and excluded nearby leads in
  Developer mode before authoring any physical connection.
- Verify both zone thresholds, Gloom Eye, Taisaijin, and Tros snapping and
  marker-only behavior on their actual floors.
- Test representative long routes, off-route recovery, and closed-door state.
- Reload only after shared catalog/page integration.
