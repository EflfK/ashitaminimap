# Inner Horutoto Ruins (zone 192)

## Routing-first completion

The exact runtime `FFXiMain.dll` stock calibration for pages `1` through `4`
remains authoritative. Four selected entrance/destination groups remain
separate because the ruins use distinct handoffs; no structure image is
required.

All four installed map-table records use scale byte `2`. Page `1` has origin
`(112, 288)`, page `2` has origin `(224, 272)`, and pages `3` and `4` share
origin `(368, 272)`.

Minimap.dll was not loaded during the live Toraimarai-side visit, so the
catalog default incorrectly kept Lily Tower page `1` active at
`(-259.600, 233.800, 4.800)`. Non-overlapping `page_rules`, derived from the
four retained graph-component footprints, now select each tower section
immediately. Exact `page_graph_seeds` provide the same component mapping as a
fallback:

- page `1`: Lily Tower / East Sarutabaruta / Survival Guide component;
- page `2`: Beetle's Burrow / Treasure Chest component;
- page `3`: Rose Tower / West Sarutabaruta component;
- page `4`: Rose Tower / Toraimarai Canal component.

The Survival Guide and Treasure Chest references carry the corresponding page
IDs. Areas excluded from this partial graph continue to fall through to normal
stock/default page selection.

## Page 2 D-10 correction overlay

The stock Beetle's Burrow artwork ends at the D-10 room even though its
walkable component continues south. Live traversal on CatsEyeXI confirmed the
player at `(-223.271, -218.457, 0.000)` after passing the Cracked Wall at
`(-220.013, -184.249, -2.072)`. Exact page-2 stock calibration places both
points immediately below the drawn room, proving an artwork omission rather
than a scale or origin error.

`192_02_structure.png` supplies a focused walkable overlay for this component.
It is generated from the pinned collision OBJ and native Detour topology with
the verified Treasure Chest seed `(-177.956, -220.058)`. The overlay retains
46 of 2,218 polygons; the remaining 159 native components stay unresolved and
the zone remains partial. Its component inventory is recorded in
`component-reports/192_02_structure.json`.

Generation command:

```text
python tools/generate_walkable_map.py \
  <xiNavmeshOBJs>/obj/Inner_Horutoto_Ruins.obj \
  <xiNavmeshes>/Inner_Horutoto_Ruins.nav \
  assets/maps/192_02_structure.png \
  --origin-x 224 --origin-y 272 --pixels-per-yalm 0.4 \
  --seed=-177.956,-220.058 \
  --component-report assets/maps/component-reports/192_02_structure.json
```

The graph-derived page-rule rectangles are:

| Page | World X | World Y |
| ---: | ---: | ---: |
| `1` | `365..475` | `95..205` |
| `2` | `-340..275`, live Z `-4..12` | `-265..150` |
| `3` | `-235..-170`, live Z `<= -10` | `5..85` |
| `4` | `-340..-225`, live Z `-4..12` | `-55..310` |

Page `2` originally stopped at world Y `-175`, the retained south-component
footprint. Live traversal through the D-10 Cracked Wall first proved that the
same stock page remains correct in the room to its north: the player at
`(-211.512, -154.576, 0.500)`, the Cracked Wall at
`(-220.013, -184.249, -2.072)`, and the Magic Gate of Horutoto at
`(-175.656, -140.005, -2.321)` all align with page 2. That focused correction
was then expanded by the continued traversal evidence below.

Subsequent attended traversal proved that the Magic Gate of Horutoto is not a
page boundary. Immediately east of it, the player at
`(-167.262, -139.543, 0.000)` remains on Beetle's Burrow's drawn corridor.
Earlier points at `(225.659, 20.593, 0.130)` and
`(32.646, -114.535, 7.440)` trace that same stock-page corridor back toward
its eastern handoff. Page `2` therefore covers the full stock-art footprint
from X `-275..275`, Y `-265..150`, at live Z `-4..12`.

Rose Tower page `3` overlaps part of that X/Y rectangle but occupies the
separate live-Z band around `-15`; its rule now requires live Z `<= -10`.
Page `4` remains earlier in rule priority for its non-overlapping northern
component. The page-2 eastern bound stops before the Cracked Wall at
`(299.997, 9.744)`, which belongs to the preceding Lily Tower handoff.

The pinned native navmesh places the Three Mage Gate approach component at
X `-300.0..-226.375`, Y `-54.733..87.517`, adjacent to the page-2 component
that ends around `(-249.0, -101.683)`. The Sealed Portal at
`(-260.071, -15.647)` belongs to that approach component. Page `4` now begins
at Y `-55` and extends east to X `-225`, using live Z `-4..12` so the
overlapping Rose Tower page-3 floor near live Z `-15` remains distinct.

Continued live traversal at `(-291.8, -140.6, 2.0)` exposed another false
boundary: page `2` fell back to Lily Tower page `1` immediately west of X
`-275`. That cutoff was only the edge of the retained graph component, not a
physical map handoff. Page `2` now extends west to X `-340`, while the earlier
page-4 rule still takes priority from Y `-55` northward. Regression points for
the full route therefore resolve as follows: `(-291.8, -140.6, 2.0)`,
`(-223.271, -218.457, 0.0)`, and `(-206.087, -109.130, 0.5)` select page `2`;
`(-258.0, -54.733, -0.406)` and the Sealed Portal at
`(-260.071, -15.647, 0.0)` select page `4`.

- Graph: 789 nodes, 849 bidirectional edges, 7 components.
- SHA-256: `79454008F41722A33645C9ECEBABB3E14650E5438794471122DEF1371C80377B`.
- Seeds: `(453,182.3,-8)`, `(-177.956,-220.058,-0.002)`,
  `(429.002,180,-12.992)`, `(-193.007,59.969,-15.057)`,
  `(-259.981,250.063,6.448)`, `(-259.996,242.859,6.399)`, plus exact
  supported-NM seeds `(-57.347,-66.473,7.631)`,
  `(-238,-167,-0.5)`, and `(-236.855,-51.263,0.476)`.
- Nav source: xiNavmeshes `d5de48de84868bde744e4864768a611e5aad82b0`,
  SHA-256 `E5236B0AEF739A3693D90DDA3B667D7567E70E40E85937DE1843E46C3FA0FA4B`.
- OBJ source: xiNavmeshOBJs `694970ce54a4fb53b69db52b8375c605686bc350`,
  SHA-256 `3C54A50577E419D327686BB967B78410869A67452A5F8640294666ED18A69D13`.

CatsEyeXI `314deaf03465f2b24b6a1e4e73a016ca036f1084` supplies the
Survival Guide and active Treasure Chest; the world catalog supplies three
physical exits. All previously supported endpoints and all five normal NM
possibilities snap within 5.70 yalms. Native-edge parity is exact and two
regenerations are byte-identical.

## Notorious-monster source audit

The exhaustive audit uses the pinned CatsEyeXI checkout above, specifically
the zone sections of `sql/mob_groups.sql` and `sql/mob_spawn_points.sql`,
`scripts/zones/Inner_Horutoto_Ruins/IDs.lua`, the complete zone `mobs`
directory, and hunt registrations in `scripts/globals/hunts.lua`.

Source SQL and script tuples are `(x, vertical, horizontal)`. Minimap points
retain source X and convert the third coordinate to world Y. Page assignments
come from the already live-verified page rules and from exact graph-component
seeds; static ranges do not claim current spawn status.

| Name | Level | Page/floor | Type | Exact starts | PHs |
|---|---:|---|---|---:|---:|
| Maltha | 22-23 | 2 / Beetle's Burrow | Timed | 1 | 0 |
| Slendlix Spindlethumb | 33-34 | 2 / Beetle's Burrow | Lottery | 1 | 1 |
| Nocuous Weapon | 25-27 | 4 / Rose Tower-Toraimarai | Lottery/random start | 3 | 3 |

Maltha uses its exact SQL start `(-57.347,-66.473,7.631)` and its script's
one-to-two-hour respawn. Slendlix preserves its one-point `spawnPoints` table
at `(-238,-167,-0.5)` and its one Goblin Leecher placeholder. Nocuous Weapon
preserves all three scripted start points in source order:

```text
(-236.855,-51.263, 0.476)
(-237.426,-23.412, 0.500)
(-230.732,-52.324,-0.025)
```

Those three Boggart starts are also the complete `phList`; the placeholder
script uses a 5% check and a one-hour minimum. Nocuous points fall within the
page-4 rule, which takes priority over page 2 in the narrow overlapping
rectangle. Maltha and Slendlix satisfy page 2.

Magicked Bones are nocturnal ordinary groups, not a separately identified NM.
The remaining records are ordinary mobs, placeholders, fished/dynamic actors,
or a single zero-position Battle Bat and are excluded. No zero-position or
distinctively named ordinary record is promoted without independent NM and
activation evidence.

## NM routing expansion and validation

The earlier four-component partial graph did not support the new endpoints:
Maltha was 352.212 yalms from retained routing, Nocuous Weapon's points were
123.181-133.755 yalms away, and Slendlix was 24.566 yalms away, just outside
the 24-yalm runtime radius. Exact supported destinations are deterministic
component-selection evidence, so the `GraphJob` now adds one seed for Maltha,
one for Slendlix, and one representative seed for Nocuous Weapon. The other
two Nocuous points belong to that same native component.

No transition, blocked link, one-way link, inferred adjacency, or mask was
added. The three newly retained native components remain separate from the
four earlier components, preserving the source's seven-component truth. The
catalog adds matching page-component seeds so automatic page selection does
not borrow a page assignment from another disconnected component.

All five endpoints pass the production snap limits:

| Endpoint | Planar snap |
|---|---:|
| Maltha | 2.649 |
| Slendlix Spindlethumb | 5.532 |
| Nocuous Weapon point 1 | 5.692 |
| Nocuous Weapon point 2 | 3.366 |
| Nocuous Weapon point 3 | 1.682 |

The complete Nocuous component routes point 1 to point 2 in 29.956 yalms and
point 1 to point 3 in 7.699 yalms. Maltha, Slendlix, Nocuous Weapon, and the
older destination components intentionally do not cross-route without source
topology; their markers remain visible and route locally where possible.

Validation for this pass:

- `tools/validate_path_graphs.py assets/paths/192.lua` passes with 789 nodes,
  849 bidirectional connections, and no one-way edge.
- The bounded audit reports seven components, production/native diff zero,
  inferred-edge delta zero, and zero candidate component pairs.
- Two generations from pinned `Inner_Horutoto_Ruins.nav` are byte-identical at
  SHA-256 `79454008F41722A33645C9ECEBABB3E14650E5438794471122DEF1371C80377B`.
- The catalog contains exactly three ranges, five points, and placeholder
  counts `0 + 1 + 3`.
