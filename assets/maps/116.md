# East Sarutabaruta (zone 116)

## Routing-first completion

The exact runtime stock calibration for page `0` remains authoritative. The
graph contains the main outdoor component and three small Horutoto entrance
corridors; no structure image is required.

- Graph: 13,290 nodes, 15,529 bidirectional edges, 1 component.
- SHA-256: `12619341F2CA03A4E04521CAAAD33ED535B7056AA74C06AF900E9C4C541653E0`.
- Seed: Windurst Woods threshold.
- Authored transitions: three direct native-neighbor seams at the north Outer,
  Inner, and south Outer Horutoto thresholds.
- Nav source: xiNavmeshes `d5de48de84868bde744e4864768a611e5aad82b0`;
  SHA-256 `F196819E81203E6AEBCB72A20F5978C98D34427DE3B55B8E0E76E15AF15059F5`.

CatsEyeXI has no Home Point, Survival Guide, or fixed standard treasure set in
this zone. All seven physical exits and all 105 supported NM possibilities
snap and interroute. Two generations and native/inferred adjacency are
byte-identical. The three narrowly scoped Horutoto seams are the only
trust-first review points.

## Notorious-monster source audit

The pinned CatsEyeXI checkout is
`314deaf03465f2b24b6a1e4e73a016ca036f1084`. The exhaustive audit used:

- `sql/mob_groups.sql` and `sql/mob_spawn_points.sql` for zone `116`;
- `scripts/zones/East_Sarutabaruta/IDs.lua` for the normal NM catalog; and
- the complete `entity.spawnPoints` and `phList` tables in
  `Sharp-Eared_Ropipi.lua`, `Spiny_Spipi.lua`, and `Duke_Decapod.lua`.

Source script tuples are `(x, vertical, horizontal)`. Minimap records retain
the first coordinate as X, use the third coordinate as Y, and retain the
source vertical coordinate as live Z evidence. The runtime layer is static
reference data only and does not imply live spawn status.

| Name | Level | Type | Exact possibilities | PHs | Evidence |
|---|---:|---|---:|---:|---|
| Sharp-Eared Ropipi | 10-11 | Lottery/random start | 50 | 4 | Full scripted `spawnPoints`; four scripted `phList` rows |
| Spiny Spipi | 9-10 | Lottery/random start | 50 | 1 | Full scripted `spawnPoints`; one scripted `phList` row |
| Duke Decapod | 8 | Timed/random start | 5 | 0 | Full scripted `spawnPoints`; 60-70 minute respawn logic |

The SQL initial rows for Sharp-Eared Ropipi and Duke Decapod are only one
possible position each; Sharp-Eared Ropipi also has a zero-position duplicate.
The script arrays are authoritative for their complete random possibilities.
Spiny Spipi likewise uses the complete script array rather than its single SQL
initial row. The three records preserve source order in `nm_spawn_ranges`.

Excluded records were reviewed explicitly:

- `Prickly Pitriv` is a level-80 Unity Wanted actor with three encounter/event
  positions, not a normal static NM range.
- `Rw Nw Prt M Hrw` is a level-95-97 Voidwatch actor; two of its three SQL
  records are zero-position placeholders.
- `Pyracmon`, Wraith Bats, cattle herds, armor actors, Astral Boxes, and slime
  actors are seasonal/event content whose zone rows are zero-position.
- `Wayward Worm`, the Cardian's Duty actors, and King of Spades are quest or
  mission records with only zero-position rows.
- Bolster is an ordinary level-1 regime mob with a zero-position row; Pixies,
  fished mobs, and the remaining ordinary groups are not NM destinations.

No unsupported or zero-position record is promoted merely because its pool
name is distinctive.

## Endpoint and routing validation

All 105 supported points satisfy the production graph's 24-yalm planar snap
radius and four-yalm floor tolerance. All three ranges belong to the graph's
single connected component, so representative cross-range routes succeed.
The existing `GraphJob` already retains these surfaces; no additional seed,
transition, blocked link, inferred edge, or graph artifact change is needed.

The existing routing certification remains unchanged: the catalog audit
reports production/native parity, and two regenerations from the pinned
`East_Sarutabaruta.nav` are byte-identical to each other and to
`assets/paths/116.lua`.

For this NM pass, all 105 source endpoints were independently checked against
the production graph. The maximum planar/elevation snap differences are
`10.977/1.425` yalms for Sharp-Eared Ropipi, `8.279/1.719` for Spiny Spipi,
and `5.218/1.266` for Duke Decapod. The map catalog contains exactly three
supported ranges and 105 points, with source counts of `50 + 50 + 5` and
placeholder counts of `4 + 1 + 0`.
