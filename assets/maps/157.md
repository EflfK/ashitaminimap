# Middle Delkfutt's Tower (zone 157)

## Routing-first completion

The exact runtime `FFXiMain.dll` stock calibration for pages `4`, `5`, `6`,
`7`, `8`, and `9` remains authoritative. The shared 3D graph keeps overlapping
floors separate; no structure image is required.

Minimap.dll can retain floor-4 page `4` after the in-zone stair transitions.
Live floor-5 state at `(18.9,57.4,-64.0)` displayed the fourth-floor artwork,
and the player then reached floor 6 at `(-15.9,25.5,-80.0)` while the stale
page condition persisted. The retained navmesh has flat-floor plateaus around
live Z `-48`, `-64`, `-80`, `-96`, `-112`, and `-128`. Automatic
`page_rules` select stock pages `4` through `9` using the midpoint boundaries
`-56`, `-72`, `-88`, `-104`, and `-120`, so stair traversal changes to the
nearest floor's stock artwork instead of inheriting page 4.

- Graph: 2,184 nodes, 2,392 bidirectional edges, 3 components (1,189, 971,
  and 24 nodes).
- SHA-256: `88A9A7F75E80110668B3A2E4B8AD6F39345830AB8E3B99F8F9664614FE0B23C9`.
- Seeds: `(-495.262,-37.750,-128)`, `(16.367,-40.372,-76.125)`, and
  `(88,73,-79)`.
- Nav source: xiNavmeshes `d5de48de84868bde744e4864768a611e5aad82b0`,
  SHA-256 `2976841949C20ECB0FE8DF6EE182072B38A03DDAA92501C866328C67268AA1E1`.
- OBJ source: xiNavmeshOBJs `694970ce54a4fb53b69db52b8375c605686bc350`,
  SHA-256 `F6CA1CA7677D953DC5D54DCA8E9C5BA41F4410C45E9A52B9ABF5725EE3897460`.

The third seed deliberately retains the small native floor-6 island containing
Rhoitos. It is a real 24-node navmesh component, not an inferred connection;
the generator now records the seed so full regeneration cannot silently drop
that supported destination. No transition joins it to another component
because the pinned data and live observations do not prove a walkable handoff.

## Notorious monsters

CatsEyeXI commit `314deaf03465f2b24b6a1e4e73a016ca036f1084` is the
pinned authority. The six active NM scripts, their `IDs.lua` registrations,
mob groups, exact spawn rows, and placeholder calls agree on this catalog.
SQL stores `(x, vertical, y)`; the table below uses live/map `(x, y, z)`.

| NM | Level | Floor/page | Live/map position | Spawn | Placeholders |
| --- | ---: | --- | --- | --- | ---: |
| Eurytos | 32 | Floor 4 / page 4 | `(20.000,94.000,-47.000)` | Lottery | 2 |
| Polybotes | 33-38 | Floor 5 / page 5 | `(-41.284,3.492,-63.636)` | Lottery | 4 |
| Rhoitos | 33-38 | Floor 6 / page 6 | `(88.000,73.000,-79.000)` | Lottery | 4 |
| Ophion | 33-38 | Floor 7 / page 7 | `(-444.000,-3.000,-95.000)` | Lottery | 3 |
| Rhoikos | 33-38 | Floor 8 / page 8 | `(-378.000,47.000,-111.000)` | Lottery | 2 |
| Ogygos | 33-38 | Floor 9 / page 9 | `(-495.000,19.000,-128.000)` | Lottery | 1 |

Each point is page-filtered and snaps to the graph: Eurytos `6.674`, Polybotes
`4.036`, Rhoitos `2.228`, Ophion `2.476`, Rhoikos `4.281`, and Ogygos `6.485`
yalms. Eurytos and Polybotes occupy the 971-node component, Rhoitos the
24-node component, and Ophion, Rhoikos, and Ogygos the 1,189-node component.

`Gerwitz's Scythe` and the two `Scythe Victim` rows are intentionally excluded.
They are special Blade of Evil quest actors in the pinned source: IDs and mob
rows exist, but this zone has no active NPC/quest trigger wiring that spawns the
encounter. Treating those dormant actors as a normal, always-available NM would
misrepresent the server behavior.

## Other overlays and verification

The same CatsEyeXI revision supplies 8 active Treasure Chests and no active
travel reference here. Every chest snaps within 7.5 yalms.

`tools/validate_path_graphs.py assets/paths/157.lua`, the six-point
`tools/validate_route_points.py` check, and
`tools/audit_path_graphs.py ../.codex-tmp/xiNavmeshes --output 157.lua` pass.
The audit reports exact native-edge parity, zero inferred-edge delta, and zero
candidate component pairs. A clean regeneration using the recorded three
seeds is byte-identical to the checked-in graph.
