# Beaucedine Glacier [S] (zone 136)

## Status and sources

Routing-first **partial** map publishing two native Detour components selected
by exact physical, travel, NM, and ordinary non-campaign spawn evidence. The
8,384-node main glacier contains both boundary regions, the Survival Guide,
Grand'Goule, and most ordinary mobs. A separate 157-node high-glacier region
is supported by thirteen ordinary Cyhiraeth spawn records. No transition,
one-way transition, blocked link, campaign, gate, transport, Maw, or drop
connection is authored.

The imported stock catalog contains only page `0`. Keep the graph zone-owned
(`page_id = nil`) and use `stock_calibration = true` pending live calibration.

- xiNavmeshes commit `d5de48de84868bde744e4864768a611e5aad82b0`
- `Beaucedine_Glacier_[S].nav` SHA-256:
  `D4B8C2094912E9BD7CF5272C82EAD538BCDE0771987BAB44A0F34F91AFB0E76A`
- CatsEyeXI commit `314deaf03465f2b24b6a1e4e73a016ca036f1084`
- Physical handoffs: `assets/world/connections.lua`; arrival, travel, NPC,
  treasure, mob-group, spawn-point, placeholder, and NM behavior: pinned
  CatsEyeXI tables, SQL, and zone scripts

CatsEyeXI SQL/script tuples are `(x, vertical, z)`; live map coordinates are
`(x, y, z) = (x, source-z, source-vertical)`. Generated graph Z is the
negation of live Z.

## Native component selection

The unseeded native graph has 13,137 nodes, 13,851 bidirectional edges, and
779 components, including 478 isolated nodes. Exact destinations plus the
40-node ordinary-spawn coverage rule retain 8,541 nodes, 9,750 edges, and two
components of sizes 8,384 and 157. The other 777 source components lack a
supported selector or are below the ordinary-coverage threshold and remain
unclassified.

The main component contains 244 floor-compatible ordinary spawn records from
non-campaign groups. The independently retained 157-node component contains
thirteen Cyhiraeth records; its first source-order selector is
`(-316.572,55.970,-100.326)`. One Ruszor record at
`(-121.623,-356.966,-7.486)` is 54.997 planar yalms from the nearest
floor-compatible source node and does not select an isolated polygon.

## Exact physical handoffs

The connection catalog supplies two paired Beaucedine-side thresholds in
source order:

```text
Batallia Downs [S] outbound (  18.084,-658.120, -4.298)
Xarcabard [S] arrival        (-180.000, 280.000,-81.850)
Xarcabard [S] outbound       (-180.000, 283.000,-86.893)
Batallia Downs [S] arrival   (  17.331,-654.230, -2.813)
```

The zone fallback arrival exactly matches the Xarcabard arrival. The two
Batallia samples and the Xarcabard arrival pass the 24-yalm planar/four-yalm
floor-aware snap rule. The Xarcabard outbound record remains marker-only: its
nearest floor-compatible node is 71.872 planar yalms away in the full source
graph and 333.851 yalms away in the retained graph. A main-component node is
only 2.720 planar yalms away but differs by 6.386 yalms in live elevation, so
floor separation is not discarded.

The Batallia direction pair routes 2.396 graph yalms. Xarcabard's paired route
is not claimed. All four records are selectors or boundary destinations only;
no cross-zone edge is inferred.

## Travel, treasure, and NMs

- Survival Guide table point `(-144.010,232.590,-80.130)`, unlock bit `95`;
  the active SQL point is `(-144.000,234.000,-80.000)`
- Home Points: verified empty
- Cavernous Maw destinations: verified empty in the active zone records
- Fixed Treasure Chests: verified empty
- Fixed Treasure Coffers: verified empty; the fifteen active named coffers are
  campaign supply/reward actors and are excluded

Publish one supported static NM:

| Name | Level | Type | Exact point | Placeholders |
|---|---:|---|---|---:|
| Grand'Goule | 83 | Lottery | `(-242.000,-127.000,-80.300)` | 3 |

Grand'Goule's script supplies the exact point and a three-placeholder list.
The Gargouille placeholder script applies a ten-percent chance and a one-hour
window after despawn.

Scylla and Came-cruse have fixed SQL positions and hunt hooks, but their
special-type groups have no pinned activation or respawn behavior suitable
for a static catalog. Becut has a zero position and zero-level metadata.
Count Halphas and its demons, Shesha, Vasuki, Kaliya, Astika, the overtuned
Orcish Bloodletter, campaign forces, fortifications, pets, helpers, and event
actors are excluded. Yilbegan, Lord Ruthven, Feuerunke, Erebus, Gjenganger,
and Gorehound are dynamic Voidwalkers and are also excluded.

The inactive `_3s0` and `_3s1` gate-side records, campaign relocation point,
campaign controls, Regal Pawprints, quest/event destinations, and possible
drops remain attended references only. None supplies a traversable endpoint
pair. The active campaign relocation destination is not a normal travel
reference and is not published.

## Deterministic generation and validation

```text
python tools/generate_path_graph.py \
  <xiNavmeshes>/Beaucedine_Glacier_[S].nav assets/paths/136.lua \
  --zone-id 136 \
  --seed=18.084,-658.120,-4.298 \
  --seed=-316.572,55.970,-100.326
```

The first selector is the first connection-catalog record and retains the
main network. The second is the first exact source-order ordinary spawn on
the otherwise-unselected 157-node region. No mask, page override, elevation
band, transition, one-way transition, or blocked link is used.

- Two native generations and one inferred generation are byte-identical.
- Graph SHA-256:
  `2168A74D1B5AACE77F9C0F5B32080556A66D8FFB2CB6B652155792A19611366A`
- `tools/validate_path_graphs.py assets/paths/136.lua` passes.
- The exact-job audit passes with zero production/native node or edge delta
  and zero native/inferred edge delta.
- The retained graph has two components, no isolated node, 16 native edges
  longer than 24 yalms, no steep edge, no separated-floor overlap, and no
  nearby-component advisory.
- Xarcabard arrival to Survival Guide routes 83.937 yalms; Survival Guide to
  Grand'Goule routes 760.419.
- All three snapping boundary samples, the Guide, Grand'Goule, and the retained
  Cyhiraeth selector snap within 0.894-6.185 planar yalms on their requested
  floors.

Attended completion requires page-0 calibration; classification of 777
unselected components and 16 long edges; resolution of the Xarcabard outbound
floor-snap gap and the unsnapped Ruszor record; live before/after checks at
both boundaries, inactive gate-side records, campaign controls and relocation,
event/quest destinations, and possible drops; and verification of the Guide,
Grand'Goule, excluded NMs/actors, routes, endpoint snapping, and off-route
recovery.

## Exact shared integration

Register:

```lua
[136] = 'assets/paths/136.lua',
```

Add this generator job in numeric zone order:

```python
GraphJob(
    "Beaucedine_Glacier_[S].nav",
    "136.lua",
    (
        "--zone-id", "136",
        "--seed=18.084,-658.120,-4.298",
        "--seed=-316.572,55.970,-100.326",
    ),
),
```

Add this catalog entry in numeric zone order:

```lua
[136] = {
    name = 'Beaucedine Glacier [S]',
    stock_calibration = true,
    travel_references = travel_reference_set({
        { kind = 'survival_guide', name = 'Survival Guide', unlock_bit = 95, x = -144.010, y = 232.590, z = -80.130 },
    }),
    treasure_spawns = {},
    nm_spawn_ranges = {
        { name = "Grand'Goule", level = '83', z = -80.300, floor = 'SURFACE', spawn_type = 'Lottery', placeholder_count = 3, points = { { x = -242.000, y = -127.000 } } },
    },
},
```

Add README source row:

```text
| Beaucedine Glacier [S] | `D4B8C2094912E9BD7CF5272C82EAD538BCDE0771987BAB44A0F34F91AFB0E76A` |
```

Add artifact row:

```text
| `136.lua` | 8541 | 9750 | `2168A74D1B5AACE77F9C0F5B32080556A66D8FFB2CB6B652155792A19611366A` |
```
