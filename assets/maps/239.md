# Windurst Walls (zone 239)

## Routing-first status

Windurst Walls uses stock page `m_239_00`, scale byte `4`, record offsets
`(-256, -320)`, world origin `(256, 320)`, and `0.80` source pixels per yalm.
The printed H-8 grid anchor remains `(255, 256)`.

The production deliverable is the display-only navigation graph and supported
destination catalog. Structure rendering is dormant during normal addon
operation.

## Preserved partial structure evidence

`239_partial_structure.png` preserves the attended 2026-07-30 structure
experiment exactly as generated. It contains 859 of 5,497 Detour polygons:
native components 22 and 395 joined by the live-verified central seam. It is
partial evidence, is not a completed structure layer, and must not be expanded
unless visible structure work is explicitly requested.

- PNG SHA-256:
  `344901E8374EAD7B87F97F91072AD919D8B49B7A66636FF163C327688DB73C0F`
- Component report:
  `component-reports/239_partial_structure.json`
- Component report SHA-256:
  `EEE86DD1D82F6D4900BAE4E10D4B28B8CA6D2CF66747C5BDDB1BC2B8F9EEA937`

Historical generation command:

```text
python tools/generate_walkable_map.py \
  <xiNavmeshOBJs>/obj/Windurst_Walls.obj \
  <xiNavmeshes>/Windurst_Walls.nav \
  assets/maps/239_partial_structure.png \
  --origin-x 256 --origin-y 320 --pixels-per-yalm 0.8 \
  --seed=31,-40 --seed=-212,-99 \
  --transition=0.128,-37.894,-5.769:2.599,-39.386,-5.234 \
  --component-report=assets/maps/component-reports/239_partial_structure.json
```

Pinned sources:

- xiNavmeshes commit
  `d5de48de84868bde744e4864768a611e5aad82b0`,
  `Windurst_Walls.nav` SHA-256
  `643C1AD0569B7D4C4BA1FD6756EB3402F06B7C995523A80A7507CB9FBA3FBAD1`.
- xiNavmeshOBJs commit
  `694970ce54a4fb53b69db52b8375c605686bc350`,
  `Windurst_Walls.obj` SHA-256
  `314C1C15E446D269C25D00C4E45F538C5954F940BFD8C8327B4EA13EEC3CBE1B`.

## Verified routing evidence

The central public-route seam was walked in both directions:

| Control | Live X | Live Y | Live Z | Native component |
| --- | ---: | ---: | ---: | ---: |
| North entrance | 0.128 | -37.894 | -5.769 | 22 |
| Midpoint | 1.826 | -38.498 | -5.625 | source gap |
| South exit | 2.599 | -39.386 | -5.234 | 395 |

The graph records this bidirectional transition:

```text
--transition=0.128,-37.894,-5.769:2.599,-39.386,-5.234
```

Focused 2026-07-30 checks also established:

- the reachable House of the Hero route stays on the lower surface from
  `(-17.638, 248.823, -12.457)` through
  `(-29.924, 256.400, -12.484)` to the door lip at
  `(-25.990, 262.374, -13.000)`;
- the elevated Priming Gate component is behind that closed door and is not a
  public-route connection;
- the apparent northwest projected seam is blocked by collision. The last
  reachable east-side controls were `(-189.632, 150.579, -2.822)` and
  `(-190.637, 147.838, -2.659)`, so the overlapping western surface remains
  disconnected; and
- the authored Windurst Waters waypoint correctly activates the zone handoff.
  The graph's last safe Walls-side snap is
  `(-193.627, 140.505, -2.340)`, 0.589 yalms from its nearest graph node.

The graph is now published from the strongest deterministic and focused live
evidence available. Further review uses Developer mode's full pathing web;
concrete defects receive targeted correction instead of speculative
seam-by-seam manual audits. None of this expands the dormant structure PNG.

## Supported static destinations

CatsEyeXI source commit
`314deaf03465f2b24b6a1e4e73a016ca036f1084` provides:

- Home Point #1: unlock index 19, `(-72.069, 124.784, -5.013)`.
- Home Point #2: unlock index 20, `(-212.000, -99.000, 0.001)`.
- Home Point #3: unlock index 21, `(31.000, -40.000, -6.500)`.
- Survival Guides: verified empty.
- Active Treasure Coffer reference: `(-214.300, -147.650, 0.000)`.
- Treasure Chests: verified empty.
- Zone mob/NM table: verified empty.

Coordinate conversion from `npc_list.sql` is
`(pos_x, vertical, horizontal)` to minimap `(x, y, z)`.
